# Minimum dependencies for standalone confidence-gated v2 runner
numpy>=1.24
scipy>=1.10
pandas>=2.0
librosa>=0.10
partitura>=1.5
scikit-learn>=1.3
soundfile>=0.12
pretty_midi>=0.2

# Matchmaker library (in-tree)
# - Cython extensions need building: pip install -e .  (from matchmaker/ root)
Cython>=3.0
setuptools>=65

# FluidSynth (system dependency)
# Ubuntu/Debian:  sudo apt install fluidsynth libfluidsynth-dev
# Arch:           sudo pacman -S fluidsynth
# macOS:          brew install fluidsynth

# Optional (for transcription validation)
piano_transcription_inference>=0.0.6
torch>=2.0
torchlibrosa>=0.1.0

# Optional (for audio-only proxy training)
joblib>=1.3
